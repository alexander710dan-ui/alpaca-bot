# Pinned so a surprise alpaca-py release can never change live-order behavior mid-flight.
# Bump deliberately: update the pin, run `python3 -m pytest -q tests/` and a --test dry run.
alpaca-py==0.43.4
